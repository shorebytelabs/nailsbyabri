name: Apply migrations to Stage

# DISABLED - Stage environment not in use
# This workflow will not run automatically on push
# It can still be run manually from GitHub Actions UI if needed

on:
  workflow_dispatch:     # Only runs when manually triggered from GitHub UI
  # push:                # Disabled - removed automatic trigger
  #   branches:
  #     - main

jobs:
  migrate-stage:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow disabled
        run: echo "This workflow is disabled. Stage environment is not in use."
